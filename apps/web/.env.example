# apps/web reads these at runtime. Set them in Vercel project settings.
DATABASE_URL=
DATABASE_URL_POOLED=
REDIS_URL=
# Local fixture scanning only. MUST be unset in production - it disables the SSRF guard.
ALLOW_PRIVATE_SCAN_TARGETS=false
